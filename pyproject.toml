[project]
name = "steward"
version = "0.1.0"
requires-python = ">=3.11"
# Memory stack is a two-step install (adapter pins exact cognee==; see requirements.txt):
#   cognee[fastembed] @ git+https://github.com/topoteretes/cognee.git@dev  # >=1.5.0.dev1
#   cognee-community-vector-adapter-qdrant==0.4.0

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["."]

# Core deps. Install Cognee (dev, remember/recall in >=1.5.0.dev1) then the Qdrant adapter.
# Adapter pyproject pins an exact cognee==, so install it separately with --no-deps.
#   uv pip install "cognee[fastembed] @ git+https://github.com/topoteretes/cognee.git@dev"
#   uv pip install cognee-community-vector-adapter-qdrant==0.4.0 --no-deps --ignore-requires-python
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0
apscheduler>=3.10.0
python-dotenv>=1.0.0
qdrant-client>=1.18.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
